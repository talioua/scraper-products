name: Scrape Products

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install packages
        run: |
          pip install -r requirements.txt

      - name: Run scraper
        run: |
          python scrape_cotepara.py

      - name: Upload CSV
        uses: actions/upload-artifact@v4
        with:
          name: csv-files
          path: "*.csv"
